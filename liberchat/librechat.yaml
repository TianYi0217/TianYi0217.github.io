version: 1.1.6

cache: true

endpoints:
  custom:
    - name: "v3"
      apiKey: "${V3_API_KEY}"
      baseURL: "https://api.gpt.ge/v1"
      models:
        default: ["gpt-4o"]
        fetch: true
      titleMethod: "completion"
      titleModel: "gpt-4o-mini"
      summarize: false
      summaryModel: "gpt-4o-mini"
      forcePrompt: false
